# General environment to execute the pipeline
# Create from the base directory of the repository using `mamba env create -f qc-pipeline/env/snp_qc.yaml`
name: snp_qc
channels:
  - conda-forge
  - bioconda
dependencies:
  - snakemake
  - plink2
  - plotnine
  - matplotlib