##
#
# This snakefile executes the QC pipeline. The different parts of the pipeline are organized in modules represented in different snake files.
# The pipeline is expected to run in the conda environment named 'snpQc' built using `auxiliaries/environment/snpQc-env.txt`.
#
# Commands to execute from the qc-pipeline/snakefiles folder:
# conda activate snp_qc
# snakemake --snakefile Snakefile --cores 16 --use-conda --rerun-incomplete
#
##

# External libraries
from pathlib import Path
import shutil
from shutil import copyfile
import gzip
import pandas as pd
import numpy as np
import plotnine as p9
import matplotlib
import math
import sys
import yaml
import io
import os
matplotlib.use('Agg')


# Config file
configfile: "parameters/config.yaml"

# Local libraries
sys.path.append('utils')  # in order to find our local packages
import mobaQcTools as mqc
import batch_report

# Pipeline variables and setup
include: "parameters/variables.py"
include: "parameters/batch_details.py"
include: "utils/setup.py"

# Ensure bash as shell
shell.executable("/bin/bash")

# A file with rule properties and ordering
with open("parameters/rules.yaml", 'r') as stream:
    rule_info = yaml.safe_load(stream)

# A bit of sanity check
for batch in batches:
    if batch not in batch_genotyping_chip:
        raise Exception(f"Batch '{batch}' not in 'batch_genotyping_chip', please edit 'batch_details.py' accordingly.")
    if batch not in batch_name:
        raise Exception(f"Batch '{batch}' not in 'batch_mobagenetics_10_name', please edit 'batch_details.py' accordingly.")

# Report (top level)
report: "report/qc.rst"

# The different modules
include: "mod1-data-conversion.snake"
include: "mod2-genetic-relationship.snake"
include: "mod3-population-clustering.snake"
include: "mod4-good-markers.snake"
include: "mod5-harmonization.snake"
include: "mod6-phasing-and-imputation.snake"
include: "mod7-post-imputation-merging.snake"
include: "mod8-release-annotation.snake"


# rule all, lists the output of every module
rule all:
    input:
        expand(github_docs / "{batch}" / "imputation_report.md", batch = batches),
        github_docs / "phasing_report_unfiltered" / "phasing_report_unfiltered.md",
        resultPath / "count_tables" / "mod5_samples",
        resultPath / "count_tables" / "mod5_variants",
        github_docs / "initial_samples_overview.md",
        mod7_merge = tmpMod7 / "mod7_merge.pgen",
        mod8_best_markers_het = tmpMod8/'mod8_best_snps.het',
        mod8_best_markers_hardy = tmpMod8/'mod8_best_snps.hardy',
        mod8_best_markers_afreq = tmpMod8/'mod8_best_snps.afreq',
        mod8_best_markers_smiss = tmpMod8/'mod8_best_snps.smiss',
        mod8_best_markers_vmiss = tmpMod8/'mod8_best_snps.vmiss',
        mod8_sex_check = tmpMod8/"mod8_check_sex.sexcheck",
        mod8_king_relatedness= tmpMod8 / "mod8_pedigree_ibd_estimate.kin0",
        mod8_mismatch_information = tmpMod8 / "mismatch_information.gz",
        mod8_mismatch_relationship = tmpMod8 / "mismatch_relationship.gz",
        mod8_pcs= tmpMod8 / "mod8_pca_both.pcs",
        mod8_cluster_file = tmpMod8/"clusters",
        mod8_ceu_id_file = tmpMod8/"ceu_core_ids",
        mod8_release_md_file= github_docs / "readme.md",
        y_and_mito = expand(str(release_folder / "Y_and_mito_variants" / "{batch}" / "release_y_mito{ext}"), batch = batches, ext = [".bed", ".bim", ".fam"]),

        r2_table = release_folder/"snp"/(release_base_name + "_snp_r2.gz"),
        best_snps = release_folder/"snp"/(release_base_name + "_reference_500k_snps"),
        pcs_with_1000G = release_folder/"pca_with_1000G"/(release_base_name + ".pcs"),
        eigenvector_with_1000G = release_folder/"pca_with_1000G"/(release_base_name + ".vec"),
        snp_loadings_with_1000G = release_folder/"pca_with_1000G"/(release_base_name + ".load"),
        eigenvalues_with_1000G = release_folder/"pca_with_1000G"/(release_base_name + ".val"),
        variance_explained_with_1000G = release_folder/"pca_with_1000G"/(release_base_name + ".pve"),
        pcs_only_moba = release_folder/"pca_only_moba"/(release_base_name + ".pcs"),
        eigenvector_only_moba = release_folder/"pca_only_moba"/(release_base_name + ".vec"),
        snp_loadings_only_moba = release_folder/"pca_only_moba"/(release_base_name + ".load"),
        eigenvalues_only_moba = release_folder/"pca_only_moba"/(release_base_name + ".val"),
        variance_explained_only_moba = release_folder/"pca_only_moba"/(release_base_name + ".pve"),
        cluster_file = release_folder/"pca"/(release_base_name + "_clusters"),
        ceu_id_file = release_folder/"pca"/(release_base_name + "_ceu_core_ids"),
        king_relatedness = release_folder/"kinship"/(release_base_name + ".kin0"),
        sex_check = release_folder/"sex_check"/(release_base_name + ".sexcheck"),
        batch_table = release_folder/"batch"/(release_base_name + "_batches"),
        confirmed_relationships = release_folder/"batch"/(release_base_name + "_confirmed_relationships"),
        readme = release_folder/"readme.md"



